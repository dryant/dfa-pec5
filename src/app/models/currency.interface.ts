export interface CurrencyInfo {
  code: string; // Código de la moneda (por ejemplo, "EUR")
  name: string; // Nombre de la moneda (por ejemplo, "Euro")
  symbol: string; // Símbolo de la moneda (por ejemplo, "€")
}
